# World Model Journal – Required Environment Variables
#
# Copy these into `.env.local` and supply your Supabase project values.
# Only the anon key is needed; workers and service-role keys are no longer used.

NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
